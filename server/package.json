{
  "name": "rulya-parser",
  "version": "1.0.0",
  "description": "Парсер статистики рулетки HWM Daily для приложения Руля",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "refresh": "node -e \"require('./parser').refreshAllRules().then(r => console.log('OK', r.updatedAt, Object.keys(r.rules).length, 'rules'))\""
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "iconv-lite": "^0.6.3"
  }
}
